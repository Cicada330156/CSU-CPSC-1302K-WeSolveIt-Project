fileType: ".recipebook"
if it is a recipe, we expect the following:
array of recipes
	type of recipe: base, baking, grilling...
	instance variables, with key the same as the variables in the class
		instance variables will be blank ("" or 0) if not instantiated
